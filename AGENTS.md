# AGENTS.md

本文档适用于仓库根目录及其所有子目录，供维护者和自动化编码代理协作使用。若更深层目录以后增加新的 `AGENTS.md`，以距离目标文件最近的说明为准。

## 项目定位

OPS-Scripts 是面向 Ubuntu 和 Debian 的交互式 Bash 运维工具集。程序通常以 root 运行，能够修改 SSH、防火墙、账号、sudoers、APT、systemd、cron、日志和 Swap 等关键系统状态。因此，安全性、可恢复性和发行版兼容性优先于代码精简。

## 目录与入口

- `install.sh`：安装器。获取最新 GitHub Release，安装到 `/opt/ops-scripts`，并创建 `/usr/bin/ops-scripts`。
- `launch.sh`：主入口。完成平台和权限检查、首次初始化、更新以及菜单调度。
- `modules/common.sh`：共享日志、输入、系统检测、镜像和依赖函数。
- `modules/init_*.sh`：首次运行时的系统和 SSH 初始化。
- `modules/*_mgmt.sh`：账号、组、cron、服务和 sudoers 管理。
- `modules/firewall.sh`：nftables、Fail2ban 和临时白名单。
- `modules/log_clean.sh`：日志清理和 logrotate 管理。
- `modules/caddy.sh`、`modules/nginx.sh`：Web 服务器安装与管理。
- `README.md`：面向用户的功能、安装和风险说明。

## 运行模型

1. `launch.sh` 使用 `set -euo pipefail`，定位 `modules/` 后加载 `common.sh`。
2. 未发现 `/etc/ops-scripts/.initialized` 时，入口依次加载 `init_system.sh` 和 `init_ssh.sh`。
3. 主菜单按需 `source` 功能模块，并调用公开入口 `run_<feature>`。
4. 模块依赖共享的颜色、日志、确认和输入函数；模块不应假设自己作为独立进程运行。

修改加载逻辑时要特别注意：被 `source` 的文件与 `launch.sh` 共享变量、函数和 Shell 选项。模块不得覆盖入口级路径变量，也不得在加载阶段产生系统变更。

## 代码规范

- 使用 Bash，不要引入仅适用于 `/bin/sh` 的兼容层。
- 文件保持 UTF-8、无 BOM；Git 中使用 LF。Windows 工作区若由 `core.autocrlf` 检出为 CRLF，不要无故批量规范化无关文件。
- 缩进使用 4 个空格，不使用制表符。
- 函数使用 `snake_case`；模块内部辅助函数可用 `_` 前缀；模块公开入口使用 `run_<feature>`。
- 常量和跨函数共享变量使用 `UPPER_SNAKE_CASE`。模块路径变量应带模块前缀，避免覆盖 `launch.sh` 的根目录变量。
- 函数内部变量默认声明为 `local`。数组必须使用 Bash 数组，命令参数不要用未加引号的字符串拼接。
- 变量展开默认加双引号；确实需要分词或 glob 时必须有邻近注释，并验证输入。
- 优先使用 `printf`；保留现有彩色输出时可以沿用 `echo -e`，但不要让用户输入成为转义序列。
- 用户可见文本和注释使用中文，命令、路径、变量和协议名称保持其原文。
- 不复制 `common.sh` 已有功能。新增通用能力放入 `common.sh`，业务逻辑留在对应模块。

## 模块约定

- 模块只能在函数内部执行有副作用的操作；顶层只允许声明变量、函数和加载依赖。
- 模块若需要自身目录，使用唯一的模块级名称，或直接基于 `${BASH_SOURCE[0]}` 加载 `common.sh`；不要重写入口的 `SCRIPT_DIR`。
- 菜单必须包含 `0) 返回`，并通过 `select_option` 校验范围。
- 破坏性操作至少需要一次明确确认；涉及 SSH、防火墙、账号删除、日志删除或覆盖配置时，应显示精确目标和影响范围。
- 所有用户输入都要按语义验证：端口须为 `1-65535`，IP/CIDR、用户名、组名、日期、数字、服务名和路径不得只做“非空”检查。
- 不要根据用户输入构造 `eval`、`bash -c` 或未加引号的命令字符串。
- 临时文件和目录使用 `mktemp`，并用 `trap` 确保所有返回路径都会清理。
- 写系统配置时采用“生成临时文件 -> 语法验证 -> 备份旧文件 -> 原子替换 -> 重载 -> 失败回滚”的顺序。
- 下载归档后先在临时目录解压，校验预期文件和目录，再切换安装目录；不得先删除可用版本。
- 认证请求头、令牌等敏感配置必须使用 `0600` 权限，日志只显示字段名，不显示值。

## `set -euo pipefail` 注意事项

入口的严格模式会影响所有被加载模块。以下命令在“正常但无结果”的情况下也可能返回非零：

- `grep`、`find` 组合和进程替换；
- `crontab -l`（用户尚无任务）；
- `systemctl status`（服务未运行）；
- `journalctl -f` 被 Ctrl+C 终止；
- 用于展示信息的管道。

预期允许失败时必须放入 `if`、`!`、`|| true` 或明确的错误处理分支。不要在执行清理命令后再检查 `$?`；应直接用 `if command; then ... else ... fi` 保存真实结果。

## 高风险改动检查清单

### SSH

- 修改前备份配置。
- 处理 `Include` 和 `Match` 块，确保全局选项写入正确作用域。
- 在替换或重启前执行 `sshd -t`。
- 验证失败必须恢复备份，不得写入初始化完成标记。
- 提醒用户保持当前连接并测试新连接。

### nftables 与 Fail2ban

- 不要在新规则验证成功前关闭现有防火墙。
- nftables 不同 base chain 的 `accept` 不是跨链的最终放行；允许规则和默认 `drop` 应在同一可控链路中组织。
- 修改前备份单个配置文件；失败时恢复该文件，而不是删除整类规则。
- 验证端口、协议、IPv4/IPv6 地址和 CIDR。
- 端口转发和 masquerade 必须限制匹配范围，不能无条件作用于所有流量。
- 使用 `nft -c -f` 验证后再重载，并在重载失败时恢复上一份可用规则。

### 安装与更新

- 固定 Release 或显示将安装的确切版本。
- 优先校验 SHA-256 或上游签名。
- 防止 tar 路径穿越，并确认解压结果包含 `launch.sh`、`modules/common.sh` 等必需文件。
- 使用同一文件系统内的原子目录切换，保留可恢复的上一版本。
- 当前安装器支持 `curl | bash`：交互输入必须继续显式读取 `/dev/tty`，无 TTY 时自动进入非交互模式，并保留可脚本化参数。修改入口判断时必须覆盖 `BASH_SOURCE[0]` 在 stdin 执行时为空的场景。

### 文件和日志清理

- 拒绝 `/`、`/etc`、`/usr`、`/var`、`/var/lib`、Home 根目录等过宽目标。
- 解析并规范化绝对路径，确认其处于允许范围内。
- 删除前列出候选文件、数量和体积；候选集与实际删除必须使用相同条件。
- 默认只删除普通文件，不跟随符号链接，不跨文件系统。
- 删除 `/etc/logrotate.d` 配置时只允许管理本项目创建并带有明确前缀的文件。

### 账号、sudoers 和 cron

- 用户或组创建失败/取消后，清理本次流程创建的中间对象。
- 删除用户前再次显示 UID、Home 和活动进程。
- sudoers 修改必须通过 `visudo -cf`，失败自动恢复。
- 修改 crontab 时保留空 crontab 场景，并在临时文件删除前保存 `crontab` 命令状态。

## 文档同步要求

以下改动必须同时更新 `README.md`：

- 主菜单编号、功能名称或入口模块变化；
- 新增、删除或重命名模块；
- 安装路径、标记文件、依赖包或支持系统变化；
- 默认 SSH、防火墙、Caddy、Nginx 或日志行为变化；
- 新的高风险操作、凭据存储方式或备份/回滚策略；
- 安装和更新命令变化。

文档只能描述代码当前确实实现的行为。不要写“自动备份”“自动回滚”“安全验证”等笼统承诺，除非每条相关路径都已实现并验证。

## 验证流程

文档或代码变更后至少执行：

```bash
bash -n install.sh launch.sh modules/*.sh
```

环境允许时再执行：

```bash
shellcheck install.sh launch.sh modules/*.sh
```

不得在开发机直接以 root 运行完整脚本作为测试。集成测试应使用可丢弃的 Ubuntu 和 Debian 虚拟机，并覆盖：

- 首次初始化、取消、失败与重试；
- 已有/没有 crontab、服务活动/停止状态；
- SSH 配置验证失败与回滚；
- nftables 规则添加、删除、无效输入、重载失败与远程连接保持；
- 安装/更新下载失败、损坏归档和中断恢复；
- 日志和配置删除的路径边界；
- 重复执行的幂等性。

纯文档改动还应检查 Markdown 相对链接、目录锚点、菜单编号、文件树以及 README 中的软件包列表是否与源码一致。

## 提交前检查

- 仅包含与任务相关的文件，没有意外的整仓换行或格式变化。
- `git diff --check` 无空白错误。
- 所有 Shell 文件通过 `bash -n`。
- 新增命令的失败状态已处理，成功日志不会掩盖真实失败。
- 高风险写入具备验证、备份、回滚和清晰确认。
- README 与真实菜单、依赖和行为保持一致。
- 没有提交令牌、真实公钥、服务器地址、日志内容或其他敏感数据。
